#define CLASSNAME CollisionHull

OBJECT_PROPERTIES
	PROPERTY_FILE(filename)
	PROPERTY_BOOL(contour)
	PROPERTY_INT(layer)
	PROPERTY_BITMASK(collisionLayers, -1)
	PROPERTY_POINT(offset)
	FACTORY_CONSTRUCTOR((state, &offset, layer, collisionLayers, filename, contour))
END_OBJECT_PROPERTIES

SELF_REGISTRATION

REGISTER_FUNCTIONS
	REGISTER_SUPERCLASS(Collidable)

END_REGISTER_FUNCTIONS

INSTANCE_OF
	SUPERCLASS(Collidable)
END_INSTANCE_OF

