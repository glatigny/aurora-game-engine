#undef OBJECT_PROPERTIES
#define OBJECT_PROPERTIES registerFactory(STRINGIFY(CLASSNAME), & Factory::JOIN(load,CLASSNAME), & Factory::JOIN(JOIN(load,CLASSNAME),Mold));

#define HEADER_FILE HEADER_TO_FILENAME(HEADER,xh)
#include HEADER_FILE
#undef HEADER_FILE
#undef HEADER

